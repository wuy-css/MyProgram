<Window x:Class="CachingDmo.SonView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CachingDmo"
        mc:Ignorable="d"
        Title="SonView" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal"  Height="50">
            <TextBlock VerticalAlignment="Center" Text="显示缓存的值："></TextBlock>
            <TextBox VerticalAlignment="Center" Width="300" Text="{Binding TextValue,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}"></TextBox>
        </StackPanel>
    </Grid>
</Window>
